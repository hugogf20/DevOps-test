apiVersion: v1
kind: Secret
metadata:
  name: kafka-secrets
  namespace: dk
type: Opaque
data:
  # These values should be base64 encoded
  username: user1
  password: QGdkzxPiqp # kubectl get secret cluster-kafka-user-passwords --namespace dk -o jsonpath='{.data.client-passwords}' | base64 -d | cut -d , -f 1
